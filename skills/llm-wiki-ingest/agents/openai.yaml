display_name: LLM Wiki Ingest
short_description: Compile any source into the wiki owner's LLM Wiki with lossless coverage and audit handoff.
default_prompt: "把这个来源按 llm-wiki-ingest 入库：保留 raw，建立 source inventory、coverage matrix、formal pages、index/log，并生成 audit handoff。"
